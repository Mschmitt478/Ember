# Local secret files

Files ending in `.txt.dist` are safe templates. Copy them to the matching `.txt`
name for local Docker Compose runs, then replace the placeholder values before
running services.

The `.txt` files are ignored by git and should not be committed.
